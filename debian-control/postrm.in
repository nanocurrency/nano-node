#!/usr/bin/env bash

set +e

systemctl stop ${CELERIX_SERVICE} 2>/dev/null
systemctl disable ${CELERIX_SERVICE} 2>/dev/null
rm -rf /etc/systemd/system/${CELERIX_SERVICE}
systemctl daemon-reload

set -e
